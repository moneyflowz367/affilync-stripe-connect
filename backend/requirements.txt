# FastAPI and server
fastapi~=0.109.0
uvicorn[standard]~=0.27.0
python-multipart~=0.0.6

# Database
sqlalchemy[asyncio]~=2.0.25
asyncpg~=0.29.0
alembic~=1.13.1

# Stripe
stripe~=7.0.0

# Authentication
PyJWT~=2.8.0

# Validation
pydantic~=2.5.0
pydantic-settings~=2.1.0

# HTTP client
httpx~=0.26.0

# Shared library
# Shared client for Affilync platform ingestion — private repo; Render builds need
# a GitHub token with read access (dev: pip install -e ../affilync-integrations-common).
affilync-integrations-common @ git+https://github.com/moneyflowz367/affilync-integrations-common.git@main

# Redis (required for production rate limiting)
redis~=5.0.0

# Development
pytest~=7.4.0
pytest-asyncio~=0.23.0
pytest-cov~=4.1.0
