[pytest]
; Tests import the service as `app.*` — the SAME tree backend/app/main.py
; itself imports (the old harness imported `backend.app.*`, a parallel
; module tree whose patches/overrides the running app never saw).
pythonpath = backend
asyncio_mode = auto
